{% extends 'base.html.twig' %}
{% block content %}
<!-- Page Content -->
<section id="mall" class="inner_page">
    <div class="container">
        <div class="box">
            <div class="center">
                <h3>{{ vendor.title }}</h3>
            </div>
            <div class="row">
                <div class="col-md-12">
                   {{ include('DesoukOnlineMallBundle:Front:User/userMenu.html.twig', { 'selected': 'product' }) }}
		            {% for flashMessage in app.session.flashbag.get('success') %}
		            	<div class="alert alert-success">
						    <a href="#" class="close" data-dismiss="alert">&times;</a>
						    <strong>{{ flashMessage }}</strong> 
						</div>
					{% endfor %}
		            <div id="vendor_edit_form">
	                    <div class="raw">
	                        <div class="panel panel-default">
	                            <div class="panel-heading">
	                                المنتجات
	                            </div>
	                            <!-- /.panel-heading -->
	                            <div class="panel-body">
	                            	<div class="user_new_form">
	                            		<ul id = "vedor_user_menu">
									    	<li><a class="btn btn-primary "  href="{{ path('front_editVendor_products_new',{'vendor':vendor.id}) }}">إضافة منتج جديد </a></li>
										</ul>
	                            	</div>
                                    <table class="table table-striped table-bordered table-hover ">
                                        <tbody role="alert">
                                        {% for product in products %}

                                            <tr>
                                                <td width="50%">{{product.name}}</td>
                                                <td><a href="{{ path('front_editVendor_products_edit',{'product_id':product.id}) }}">تعديل</a></td>
                                                <td><a href="{{ path('front_vendor_deleteProduct',{'vendor':vendor.id,'product_id':product.id}) }}">حذف</a></td>
                                            </tr>
                                        {% endfor %}

                                        </tbody>
                                    </table>
	                            </div>
	                            {{ knp_pagination_render(products) }}
	                            <!-- /.panel-body -->
	                        </div>
	                    </div>
						
		            </div>
                </div>
            </div>
        </div>
    </div>
    <!-- /.container -->
</section>
{% endblock %}