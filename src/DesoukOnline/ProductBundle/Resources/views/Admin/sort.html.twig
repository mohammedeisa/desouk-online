{% block field %}
    {% spaceless %}
        <span id="sort-arrows-container">
        <a href="{{ path('cat_tree_up', {'id': object.id}) }}">
            <i class="glyphicon glyphicon-upload"></i>
        </a>
        <a href="{{ path('cat_tree_down', {'id': object.id}) }}">
            <i class="glyphicon glyphicon-download"></i>

        </a>
        </span>
    {% endspaceless %}
{% endblock %}